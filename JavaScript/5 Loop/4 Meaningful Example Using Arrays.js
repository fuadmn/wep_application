// Meaningful Example Using Arrays

const classStudents = ["Alice", "Bob", "Charlie", "Ali"];

// length // waxa ku sheeqaa cabirka arrayda
for (let i = 0; i < classStudents.length; i++) {
  console.log(classStudents[i]);
}
